# Workload Identity: this KSA acts as the opswarden-agent GSA (Secret Manager access), no keys.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: opswarden-agent
  namespace: opswarden
  annotations:
    iam.gke.io/gcp-service-account: opswarden-agent@opswarden-lab.iam.gserviceaccount.com
